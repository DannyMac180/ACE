# ACE Week-One Launch Recovery Brief

Use this brief if the Sunday, March 15, 2026 `20:00` ET gate was not recorded
as `go` with evidence before Monday, March 16, 2026 launch operations begin.

This is a recovery path, not a replacement launch strategy. The goal is to
protect message quality, tracked attribution, and follow-up discipline without
shipping an untracked or half-prepared launch unit.

## Trigger

Switch to this brief when any of the following is true:

- the `Sunday Gate Decision` row in
  `plans/2026-03-16-cmo-launch-operations-log.md` is still `pending` or
  `blocked` after `20:00` ET on Sunday, March 15, 2026
- one or more rows in the `Sunday Preflight Completion Receipt` still lacks
  evidence when Monday, March 16, 2026 operator work begins
- the Monday anchor thread is not loaded with its exact tracked URL and thread
  shape by the time Dan needs to make the live ship or slip call

## Recovery Objective

- keep the README-first category message intact
- preserve exact tracked URLs and governed assets
- avoid compressing the week so tightly that proof and adoption blur together
- keep every publish, reply, and DM motion measurable inside the existing
  launch log

## First Recovery Block

Run this at the first live operator session on Monday, March 16, 2026 before
any post goes live.

1. Update the `Sunday Preflight Completion Receipt` with actual status for each
   incomplete row. Use `completed` only with evidence. Use `blocked` when the
   proof is still missing.
2. Update the `Sunday Gate Decision` row the same way. Do not leave it
   `pending` once Monday recovery work starts.
3. Record the exact missing surfaces. Name them directly: profile surface,
   calendar import, Monday draft, Wednesday draft, Friday draft, Tuesday
   watchlist, or Thursday watchlist.
4. Re-check only the surfaces required to decide whether Monday can still ship.
   Do not rewrite the copy pack or invent new tracked links.

## Minimum Same-Day Ship Standard

Monday, March 16, 2026 can still ship the anchor thread only if all of the
following are true by `14:00` ET:

- the governed week-one X bio and `profile_w1_readme` website field are live
- the Monday anchor thread is loaded as a connected `9`-post draft with the
  exact `w1_thread_readme` URL
- the launch calendar is imported or the Monday plus Tuesday plus Wednesday
  operating windows are manually blocked in Eastern Time
- the Tuesday distribution watchlist has at least `5` live targets
- the operator can stay available for the immediate QA check, `+30m` reply
  pass, `+3h` review, and same-day log update

If all five conditions are true, publish in a recovery window between `14:30`
and `15:00` ET on Monday, March 16, 2026, then keep the Wednesday, March 18,
2026 proof post and Friday, March 20, 2026 adoption post on their existing
dates.

## Slip Rule

If the minimum same-day ship standard is still not met by `14:00` ET on Monday,
March 16, 2026:

- do not force a late Monday post from memory
- shift the anchor thread to Tuesday, March 17, 2026 between `11:00` and
  `11:30` ET
- shift the proof post to Thursday, March 19, 2026 between `11:00` and
  `11:30` ET
- shift the adoption post to Monday, March 23, 2026 between `10:30` and
  `11:00` ET
- use Wednesday, March 18, 2026 for distribution and follow-up on the anchor
  thread instead of publishing a second public unit
- log the date changes in the `Launch Change Log` before the rescheduled post
  goes live

This keeps one clear category post, one clear proof post, and one clear
adoption post instead of collapsing all three motions into the same crowded
window.

## Logging Rules During Recovery

- paste the live post URL into the raw capture sheet within `5` minutes of
  publish
- update the canonical launch log the same day with the live URL, first metric
  snapshot, strongest objection, and next follow-up
- log every qualified public reply burst or DM thread within `24` hours even if
  the publish dates shift
- if the schedule slips, record the new dates in the `Launch Change Log` and
  the actual blocker in the `Launch Contingency Log`

## Expected Outcome

The recovery path is successful if ACE still gets:

- one clean category post tied to README
- one inspectable proof post tied to the demo asset
- one adoption post tied to the getting-started example
- same-day logging after each publish
- a dated next step for every qualified builder conversation
