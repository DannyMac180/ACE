# ACE Sunday Preflight Command Card

Date: 2026-03-14
Owner: CMO
Operator: Dan

## Purpose

Use this as the one-screen operator surface for Sunday, March 15, 2026.

This file does not replace the canonical launch log. It compresses the seven
Dan-owned preflight tasks into one execution order so the `20:00` ET gate can
be cleared without hopping across long docs.

Canonical proof still lands in:

- `plans/2026-03-16-cmo-launch-operations-log.md`
- `plans/2026-03-13-cmo-week-one-live-capture-sheet.md`

## Open These First

Keep these surfaces open before touching X or the calendar:

1. `plans/2026-03-16-cmo-launch-operations-log.md`
2. `plans/2026-03-13-cmo-week-one-live-capture-sheet.md`
3. `plans/2026-03-13-cmo-week-one-profile-surface.md`
4. `plans/2026-03-13-cmo-week-one-publishing-surface.md`
5. `plans/2026-03-13-cmo-week-one-response-bank.md`
6. `plans/2026-03-15-cmo-week-one-launch-calendar.ics`

## Sunday Execution Order

Do these in order. Do not skip ahead.

### 1. Apply the governed profile surface

- update the X bio from `plans/2026-03-13-cmo-week-one-profile-surface.md`
- update the website field to `profile_w1_readme`
- do not change the current pin yet

Done means:

- live bio matches the governed week-one wording or the logged shorter variant
- live website field uses the exact `profile_w1_readme` URL

Proof to capture:

- live profile URL or a dated note in the live capture sheet

### 2. Record the audience baseline immediately

- after the profile update, capture starting follower count
- capture the starting profile-visits reading from the exact analytics surface
- paste the timestamp and source surface into the raw baseline row first

Done means:

- the raw `Audience Baseline Snapshot` row is filled in the live capture sheet
- the canonical `Audience Baseline Snapshot` row is filled in the launch log

### 3. Import and verify the week-one calendar

- import `plans/2026-03-15-cmo-week-one-launch-calendar.ics`
- confirm Monday, Wednesday, Friday publish windows exist
- confirm Tuesday and Thursday pass windows exist
- confirm the `+30m`, `+3h`, and `17:30` ET check blocks exist

Done means:

- every imported block shows in Eastern Time
- no week-one motion still depends on manual memory

Proof to capture:

- calendar name, import confirmation, or screenshot note

### 4. Load the three governed drafts

- paste the Monday anchor as one connected `9`-post thread
- paste the Wednesday proof post with `docs/assets/ace-proof-demo/still.svg`
- paste the Friday adoption post with `w1_adoption_example`
- do not rewrite tracked URLs

Done means:

- all three drafts exist in the live composer or scheduler
- Monday is visibly a connected `9`-post thread
- Wednesday keeps the approved proof still attached

Proof to capture:

- scheduler name or draft confirmation for each row in the receipt

### 5. Validate the Tuesday and Thursday watchlists

- start from the URL-backed `2026-03-14 search note` rows in the live capture
  sheet
- keep at least `5` live Tuesday targets
- keep at least `5` live Thursday targets
- prefer warmer Monday or Wednesday engagers if they already exist by then

Done means:

- each validated row has a live post URL or a dated search note from the last
  `72h`
- no watchlist is marked ready from handles alone

### 6. Complete the Sunday receipt

- fill every row in `Sunday Preflight Completion Receipt`
- use `completed` or `blocked`; do not leave blanks
- attach the evidence surface in the row itself

Done means:

- all seven rows in the canonical receipt have timestamps, evidence, and a real
  status

### 7. Run the `20:00` ET go-or-no-go gate

- check whether all seven receipt rows are `completed`
- if yes, mark the raw gate row `go` and note that Monday can run on the
  default window
- if no, mark the raw gate row `blocked`, name the exact missing item, and set
  the next Monday check time
- transfer that same final call into the canonical `Sunday Gate Decision`
  section of `plans/2026-03-16-cmo-launch-operations-log.md`
- if the gate is `go`, freeze the governed profile surface, tracked URLs,
  proof visual, and loaded drafts; any later edit must land in the `Launch
  Change Log` before launch continues

Done means:

- the raw gate row and canonical gate row match exactly
- CEO can see one unambiguous state in the launch log

## Evidence Standard

Use these standards when deciding whether a row is complete:

| Row | Minimum acceptable evidence |
| --- | --- |
| profile surface | live profile URL or a dated note that the exact bio plus website field are live |
| audience baseline | timestamp, starting follower count, starting profile-visits reading, and source surface |
| calendar import | import confirmation or screenshot note showing Eastern Time blocks |
| Monday draft | scheduler or composer note that confirms a connected `9`-post thread |
| Wednesday draft | scheduler or composer note that confirms the still is attached |
| Friday draft | scheduler or composer note that confirms the worked-example CTA stayed exact |
| Tuesday watchlist | `5` current rows with live URLs or dated search notes |
| Thursday watchlist | `5` current rows with live URLs or dated search notes |

## Blocked-State Rule

If any row is blocked:

- write the blocker in the canonical receipt the same night
- name the exact missing surface, asset, or confirmation
- set the next Monday check time before exiting
- do not call launch ready from memory or intent

## Monday Reminder

Sunday preflight is not the end of launch control.

On Monday, March 16, 2026:

- run `Immediate Publish QA` within `5` minutes of the anchor thread going live
- pin the Monday thread only after QA passes
- capture `+30m`, `+3h`, and `17:30` ET snapshots the same day
